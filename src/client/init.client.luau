--!strict
-- Client bootstrap. W1-01 wires Loader to auto-require Controllers/ (context/02 §4).
-- W0-01 ships a minimal stub so the skeleton builds and the CI gates execute against real code.
print("[bullet-heaven] client bootstrap OK")
