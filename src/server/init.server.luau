--!strict
-- Server bootstrap. W1-01 wires Loader to auto-require Services/ (context/02 §4).
-- W0-01 ships a minimal stub so the skeleton builds and the CI gates execute against real code.
print("[bullet-heaven] server bootstrap OK")
